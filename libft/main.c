#include "libft.h"

int main()
{
	char str1[100] = "hello world";
	char str2[100] = "hd elo";
	char * ptr1;
	char * ptr2;

	 ft_strtrimstart(str1, str2);
	// ptr1 =  ft_strjoin(NULL, str2);
	// printf("%s\n",ptr1);
	return 0;
}